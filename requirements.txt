# --- LuminAria app dependencies ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx>=0.27.0
pydantic>=2.0
ffmpeg-python>=0.2.0
sse-starlette>=2.0.0
soundfile>=0.12.0
einops>=0.8.0

# --- stable-audio-tools (use [train] extra for full model-loading deps) ---
stable-audio-tools[train]
